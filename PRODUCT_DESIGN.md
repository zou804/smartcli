# SmartCLI 产品设计

## 1. 产品定位

SmartCLI 是一个安全、可审计、模型可替换的本地代码变更 Agent。它面向需要使用 AI
处理真实代码、同时关心权限边界、验证证据和数据控制的开发者与团队。

核心承诺：给定一个开发任务，SmartCLI 在明确授权范围内读取仓库、制定计划、修改文件、
执行批准的项目检查，并报告完成证据和未验证风险。

SmartCLI 不以通用聊天或无限制 shell 为竞争点。产品差异化来自：

- 默认只读和逐项能力授权；
- 结构化、可审计的工具调用；
- 本地模型与云模型可替换；
- 修改、验证、报告组成的闭环；
- 面向团队策略和合规场景的扩展能力。

## 2. 目标用户

- 维护私有仓库、希望控制代码外发范围的个人开发者；
- 不愿向 AI 开放任意 shell 权限的小型研发团队；
- 需要保存 Agent 审批与执行证据的安全敏感团队；
- 需要在 Ollama、OpenAI 兼容服务和不同供应商之间切换的用户。

## 3. 核心任务闭环

```text
任务输入
  -> 仓库检查
  -> 计划与风险说明
  -> 用户授权
  -> 最小文件修改
  -> 受控项目检查
  -> 根据观察继续修正
  -> 最终结果与审计证据
```

首期支持工作区读取、笔记检索、只读 Git、受控文件写入和枚举化 Python 项目检查。
检查工具不是通用 shell；执行仓库测试可能运行项目代码，因此需要 `--allow check` 和逐次确认。
检查可通过 Local 或 Docker 后端运行。Local 会过滤敏感环境变量但不提供操作系统隔离；Docker
默认关闭网络、使用非 root 用户、裁剪 capabilities 并限制资源，且失败时不会静默降级到 Local。

## 4. 权限模型

| 能力 | 默认状态 | 风险 | 授权方式 |
| --- | --- | --- | --- |
| 工作区列表/读取 | 开放 | safe | 无需授权 |
| 本地笔记搜索 | 开放 | safe | 无需授权 |
| 只读 Git | 关闭 | safe | `--allow git` |
| 文件创建/覆盖 | 关闭 | review | `--allow write` + 逐次确认 |
| 项目检查 | 关闭 | high | `--allow check` + 逐次确认 |

通用 shell、依赖安装、网络访问、Git 写操作和文件删除仍不开放。

## 5. 首期非功能目标

- 单个 Agent 任务和 Chat 历史具有明确上下文预算；
- 笔记与配置更新在多进程下不丢失已提交修改；
- 有副作用的动作在执行前必须留下授权审计；
- 执行完成后的审计故障不得把成功动作伪装成执行失败；
- Windows、Linux 和 Python 3.11+ 保持自动化验证。

## 6. 交付阶段与路线

### 阶段二：可信修改（0.8.0 已完成首版）

- 精确文本 Patch 工具，使用文件哈希和出现次数作为前置条件；
- 每次运行生成 `run_id` 和结构化验证报告；
- 文件写入前保存本地检查点，支持哈希保护的运行撤销；
- 使用 token 估算替代字符预算，并支持历史摘要。

检查点会包含修改前正文，保存在用户数据目录的 `smartcli/runs` 中，不写入审计日志，也不会由
`run show` 输出。执行 `run undo` 前必须验证当前文件仍等于 Agent 写入结果，避免覆盖用户后续修改。

### 阶段三：Agent Engineering Core（0.9.0 已完成）

- 仓库级 `smartcli.toml` 约束检查、可写/保护路径与执行资源；
- Local/Docker 执行后端，Docker 不可用时拒绝降级；
- 基于最后写入后真实检查动作的验证状态；
- 不记录内容的运行遥测和供应商 token usage；
- 一次性 workspace、脚本化模型与确定性 graders 组成的离线评测。

后续增强包括 Node/Go 验证模板、敏感信息扫描、模型数据外发预览和更多隔离运行时。

### 阶段四：团队产品

- GitHub/GitLab PR 与 CI 集成；
- 集中式策略、审计检索和成本统计；
- 共享仓库知识；
- SSO、RBAC 和私有模型网关。

## 7. 产品指标

- 任务测试通过率与 Patch 接受率；
- 未验证却声称完成的比例；
- 平均工具调用次数和人工确认次数；
- 越权动作数与提示注入防御通过率；
- 每个成功任务的时间、Token 和模型成本；
- 7 日、30 日重复使用率。
