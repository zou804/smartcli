[execution]
backend = "local"
network = "none"
timeout_seconds = 120
memory_mb = 512
cpus = 1.0
pids_limit = 128

[workspace]
writable = ["src/**", "tests/**", "docs/**"]
protected = [".github/**", "migrations/**"]

[checks]
allowed = ["tests", "lint", "compile"]
required = ["tests", "lint"]
